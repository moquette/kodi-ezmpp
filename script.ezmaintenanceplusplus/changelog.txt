v2026.08.30.2
- New: Settings Profile. One menu item sets up a fresh box the standard way in one
  pass: web and remote control on, add-ons allowed from any source, the Tony.7.Bones
  repository, the KodiShare and KodiBackup sources, this add-on's backup folder and
  the usual defaults. One confirmation up front, one result message at the end.
- Run it again any time. It changes only what has drifted, tells you when the box
  already matches, and never removes anything.
- Apple TVs keep their remote-control setting as it is: the profile leaves the event
  server off there on purpose.
- After Kodi reopens, the add-on checks once that the new media sources arrived and
  tells you.

v2026.08.30.1
- Restoring a backup made on a different version of Kodi now brings back your settings
  and your data, leaves the add-ons stored in the backup alone, and tells you why in
  one message, with the advice to reinstall your add-ons from their repositories.
- Every backup now records which version of Kodi made it, and the Verify backup
  archive report shows it. A backup made on the same version of Kodi restores exactly
  as it always has, add-ons included. Settings-only backups are not affected.

v2026.08.19.3
- Removes the Home Menu option added in 2026.08.19.1 and fixed in 2026.08.19.2. Both of
  those releases are withdrawn.
- The Home Menu tab is gone from this add-on's settings, and the "Send Home > Movies
  straight to POV (stock Estuary only)" option is gone with it.
- Nothing in the add-on watches the home screen any more. Pressing Movies on Kodi's home
  screen does exactly what Kodi itself does, on every skin and every box, which is what
  it did before 2026.08.19.1.
- There is nothing to switch off and nothing to undo. A box that had the option turned on
  goes back to normal on its own when this version installs, with no restart.
- Your backups, settings and custom menu are untouched.

v2026.07.31.1
- The video cache buffer returns to Kodi's own default of 20 MB when you restore a
  backup or run a Fresh Start. Before this, a restored box kept the buffer it had been
  using, and a wiped box could keep it too. Neither is right on a fleet where the boxes
  are not the same machine: a buffer that suits one device is the wrong size on another,
  and it was arriving without anyone having asked for it.
- No inherited number survives either of those two actions. Not the one carried in the
  backup archive, and not the box's own previous one.
- The per-device recommendation has not moved. It is still under Video Cache Buffer in
  this add-on's menu, where "Use recommended for this device" fills in the size that box
  should have, and you can type your own instead.
- Nothing asks you about the buffer during a restore or after one. There is no new
  prompt anywhere.
- A buffer you set yourself stays set. Nothing puts it back at start-up, on an update or
  after a restart, until the next restore or Fresh Start.
- Your box still keeps its own device name through a restore, exactly as it did before.

v2026.07.31.0
- New icon. The picture beside EZ Maintenance++ in Kodi's add-on list now carries the
  "++" next to the "e", so it matches the add-on's name. The artwork is otherwise the
  same shield it has always been.
- Nothing else is in this release. No setting, no menu and no button behaves any
  differently, and your backups, settings and custom menu are untouched.

v2026.07.22.3
- The folder shown beside Backup and Restore is readable again. With the folders this
  fleet actually uses, the folder ran off the end of the row, and the row you were
  pointing at was the worst one: Kodi slides a highlighted row's text sideways when it
  does not fit, so the word "Backup" drifted into the middle of the path and you could
  not tell where the row began or ended.
- A folder too long for the row is now shortened in the MIDDLE, keeping the share it is
  on and the last folders of the path. The Fire TV folder now reads
  "nfs://192.168.7.2/.../Kodi/Backup/fireos/" instead of running off the row, so it is
  still plainly the fireos folder and not the tvos one. Only whole folder names are
  kept, so a name is never cut in half, and nothing slides any more whether the row is
  highlighted or not.
- The full folder is unchanged and still shown in one piece where you set it, on the
  Backup/Restore tab of Settings, which the Settings row of the same menu opens.
- Short folders, "Not set" and Dropbox are untouched, and the folder on the row is
  still exactly the folder the action will use.

v2026.07.22.2
- When Backup or Restore says "Not set" and you pick it, EZ Maintenance++ now opens
  Settings ON the Backup/Restore tab, where that folder is set. It used to open
  Settings on the Maintenance tab, so you were told to set a folder and then left to
  find the right tab yourself. The same goes for Verify Backup Archive.
- The folder shown beside Backup and Restore is now exactly the folder the add-on
  will use. On a network share set up through Kodi's own browse screen, the row was
  showing a ":2049" that the add-on drops before using the folder.
- A folder setting containing only spaces now counts as not set, everywhere. The menu
  row already called it "Not set", but Backup and Restore treated it as a real folder
  and pushed on, so the failure came later and said nothing useful.
- On a slower box the Backup/Restore menu could reopen on top of the Settings window
  it had just sent you to. It now steps aside every time, however long the Settings
  window takes to appear.
- Housekeeping: a leftover start-up step that computed a code fingerprint for a
  verification tool deleted last week has been removed. Nothing read it.

v2026.07.22.1
- The backup and restore folders in the Backup/Restore menu now show on EVERY skin,
  including the built-in Estuary that Fresh Start requires. 2026.07.22.0 showed them
  only on Estuary 7 1.0.77 or newer, and said so in its release note; that limitation
  is gone and the note no longer applies.
- Each row carries its folder beside the name, so Backup shows where the archive will
  be written and Restore shows where it will be read from.
- No skin is involved in drawing them any more. An add-on's screens should never
  depend on which skin a box is running.

v2026.07.22.0
- "Set up this box" is removed. Adding media sources is what Kodi's File Manager is for,
  and "Device Name" only set the name Kodi itself sets under Settings, Services, General.
  Weather setup, the RSS ticker and "Set up everything" are gone with it.
- The Backup/Restore menu shows the backup folder while Backup is highlighted and the
  restore folder while Restore is, along the bottom of the window. Needs skin.estuary7
  1.0.77 or newer; other skins show their usual item count.
- The Backup/Restore menu has a Settings entry that opens the Backup/Restore settings tab.
- Its entries are Title Case now, not capitals.
- Fresh Start's message for an unsupported skin is two short lines.

v2026.07.21.5
- Restore no longer overwrites this box's own EZ Maintenance++ settings with the copy
  stored in the backup. Restoring another box's archive, or an older one from this box,
  used to hand this box the other box's backup folder and device name. Your own settings
  now always survive a restore.
- Restore no longer replaces EZ Maintenance++ itself with the copy inside the backup.
  Restoring an older archive put the older add-on back, so the next start silently ran
  it and undid every fix released since. The installed version now stays put.
- The two Fresh Start options are now called "Keep file manager sources" and
  "Keep repositories".

v2026.07.21.4
- Follow-up to 2026.07.21.3, from an independent review of that release.
- Fresh Start had one path left where a wipe that failed part way through left Kodi
  running on a half-erased install, and told you "nothing was removed" when in fact
  most of it had been. That is the same crash 2026.07.21.3 fixed everywhere else. It
  now closes the box and reports honestly that the wipe did not finish.
- A Restore was keeping any folder named Database from the old install, at any depth,
  including add-ons' own. It now keeps only Kodi's real database folder, which is the
  one it has to keep, and the leftovers go as they always did.
- A backup was skipping any file starting with Textures inside any folder named
  Database, which could quietly drop an add-on's own data from a full backup. It now
  only skips Kodi's own texture cache.

v2026.07.21.3
- Fixes a crash. Taking a backup could kill Kodi several minutes later, with no
  warning and nothing on screen: the box just vanished to the launcher. Clearing
  thumbnails deleted a database file that Kodi still had open, and Kodi kept running
  until the next time it needed that file. Thumbnails are now cleared through Kodi
  itself, so nothing is pulled out from under it.
- The same mistake was in two other places, and both are fixed. A Restore no longer
  deletes the databases while Kodi is running (the backup being restored puts them
  back anyway). Fresh Start still clears them, because a clean slate has to be clean,
  but it now always closes Kodi when it finishes.
- Fresh Start's Later button is gone. It left the box running on a freshly wiped
  install, where it would crash within seconds. The finishing message now just tells
  you the box is closing.
- A Restore also no longer removes the file that keeps EZ Maintenance++ enabled, so
  the add-on comes back switched on rather than looking like it disappeared.
- Backups no longer carry the thumbnail database. It is rebuilt automatically and
  never shrank once it had grown, so it was pure dead weight in every archive.

v2026.07.21.2
- One-Tap Restore has been removed. The pinned-backup shortcut is gone from the main
  menu, and its hidden pin slots are gone from Settings. Your actual backup files are
  untouched, and every one of them can still be restored the normal way, from
  Backup/Restore.
- Nothing about backing up, restoring, or Fresh Start changed. The wipe that Fresh
  Start and Restore share is the same code as before, byte for byte, so a restore
  behaves exactly as it did.

v2026.07.21.1
- Fresh Start now shows a real progress bar while it wipes, counting through the
  files, instead of a bar that sat still and looked frozen.
- When the wipe finishes, Fresh Start tells you the box needs to close and gives you
  Shut down and Later buttons, instead of the app disappearing with no word.
- Fresh Start runs only from the built-in Estuary skin, so that finishing message can
  always be shown after the wipe. From any other skin it asks you to switch first.
- A wipe no longer leaves behind stray settings for the skin you were using.
- New Fresh Start tab in Settings, with two options that are off by default: keep your
  File Manager sources (your NFS and SMB shares and their saved passwords) and keep
  your repositories, so a reset does not make you add them all back by hand.
- Adding media sources under "Set up this box" now offers to close Kodi so the shares
  actually appear, instead of only saying they were added while Kodi kept showing the
  old list until a restart.

v2026.07.19.8
- Cancelling out of a backup, a restore, or an archive check now takes you back to
  the Backup/Restore menu instead of the main screen. Backing out of the file list,
  or closing the report at the end of an archive check, used to drop you all the way
  out to the main menu - so checking a second backup file meant walking back in from
  the top every time.
- The same applies to the Full Backup / Addons Settings question: backing out of it
  returns to Backup/Restore, and still takes no backup.
- Backing out of the Backup/Restore menu itself is the way out.
- Two deliberate exceptions, so the menu never gets in the way. A restore that
  actually ran closes the menu, because the next thing to do is close and reopen
  Kodi, and because a box mid-restore is not one to take a fresh backup of. And if
  a backup, restore, or archive check sends you to Settings to choose a folder
  first, the menu steps aside rather than reopening on top of it.
- Nothing about backing up, restoring, or checking an archive changed. This is a
  navigation fix only; no backup, setting, or custom menu is affected.

v2026.07.19.7
- "Verify backup archive" has moved into Backup/Restore. It is now the third and
  last choice there, after Backup and Restore. It used to live in a Tools folder,
  and that folder is gone: when the stale-key repair was retired last release,
  Tools was left holding this single item - a folder you had to open to find one
  thing, and that one thing is plainly a backup job. It now sits with the rest of
  the backup work. It still only reads a backup file and reports what is inside;
  it never restores anything.
- "Device Name" has moved off the main menu and into "Set up this box". It is the
  first item there, above "Set up everything". Naming a box is something you do
  when setting one up, so it now sits with the other setup steps instead of on its
  own at the top level.
- Nothing else changed. Both items behave exactly as they did before, and no
  backup, setting, or custom menu is affected by this release.

v2026.07.19.6
- Finishes the message change 2026.07.19.5 started. That release updated the
  start-up message but left the restore dialog still saying "this box needs
  attention - open EZ Maintenance++", which pointed at a repair that had just
  been removed from the Tools menu. Both messages now say the same thing:
  restore again, or restart the box. Each of those re-runs the repair by itself.
- No behaviour changed. The same condition is detected, at the same moment, and
  reported to the same place; only the wording of the one stale message moved.

v2026.07.19.5
- Apple TV: a restore no longer ends with "this box needs attention" when the box
  is fine. The check was counting the skin's own menu-protection copies as a
  problem. Those copies are deliberate - they are what keeps your custom menu from
  being lost - so they are no longer reported as a fault. A real problem is still
  reported, exactly as before.
- If a restore does leave a setting unapplied, the message now says what to do
  about it instead of sending you into the add-on.
- Removed "Purge stale tvOS keys" from the Tools menu. That repair already runs on
  its own during every restore and after an update, so there was nothing for anyone
  to decide. Nothing about the repair itself changed.

v2026.07.19.4
- A restore now keeps this box's own device name and video cache buffer instead of
  copying the backup's. Those two settings describe the box itself, not the backup:
  the name is what this device is called on your network, and the buffer is sized
  for this device's memory. A restore used to overwrite both with the source box's
  values and then, on the next start, ask you to put them back.
- Those two questions are gone. Nothing is asked after a restore any more. The
  settings are simply correct, including on a brand-new box, which Kodi already
  names "Kodi".
- Both remain yours to change at any time: Device Name and Video Cache Buffer are
  items in the add-on's own menu. Device Name is new there; it used to be reachable
  only by answering the post-restore question.

v2026.07.19.3
- Internal: the add-on now clears its own stale compiled-code cache at startup.
  Python can keep running an older compiled copy of a file when the new one is
  the same size, and because builds here use a fixed timestamp for
  reproducibility, that check could not tell the two apart. Clearing the cache
  means the code that runs is the code that shipped. No change to anything you
  see or use.

v2026.07.19.1
- Internal: the add-on now reports a fingerprint of its own installed
  storage-handling code, and the release check compares that against the code
  being certified. Previously the check trusted the version number, which does
  not change when that code does, so a box running an older build could be
  recorded as verified. No change to anything you see or use.

v2026.07.19.0
- A restore that switches to a different skin now tells you. Kodi only applies a
  skin change when someone confirms it on screen, and that confirmation cannot be
  answered safely from inside a restore, so the box reopens on the skin it was
  already running. The restored skin and all of its settings are installed and
  intact - nothing is lost - but the switch has to be finished by hand in
  Settings > Interface > Skin. The next start now checks for this and says so
  instead of leaving it to be noticed. Restores that do not change the skin, which
  is the normal case, are unaffected and stay silent.

v2026.07.18.2
- Backing up to a location inside Kodi's own storage now warns first. On Apple
  TV everything Kodi owns lives in an area the system may clear to free space,
  and a wipe or reinstall removes it, so a backup saved there can be gone at the
  moment it is needed. The browse dialog offers that location as its starting
  point, which made it the easiest one to pick by accident. USB, network and
  Dropbox destinations are unaffected and are not warned about.

v2026.07.18.1
- Restoring a skin setting whose value contains a comma no longer corrupts it.
  Values are passed to Kodi's Skin.SetString builtin, and Kodi splits builtin
  parameters on commas, so a label like "Movies, HD" arrived truncated to
  "Movies". Because the truncated value was set in live memory, Kodi's
  clean-shutdown flush then wrote it over the correctly restored file: the step
  that exists to protect restored settings was corrupting them instead. Values
  are now quoted and escaped, so commas, quotes, backslashes and parentheses all
  survive exactly as archived, including leading and trailing spaces.
- A restored skin setting whose ID contained a parenthesis or a quote could hang
  a restore. Such an ID collapsed the builtin to its one-argument form, which
  opens an on-screen keyboard and blocks until answered, on a TV with nothing
  expecting input. IDs are now validated and unsafe ones are skipped.
- The hardware-verification gate can no longer be satisfied with stale evidence
  for one device class. Each verified device now records the exact storage-code
  fingerprint it was tested against, instead of the artifact carrying one shared
  value that a later run of a different device would silently re-stamp.

v2026.07.18.0
- Post-restore prompts no longer lose what you typed. A destroyed dialog is
  indistinguishable from a deliberate answer through Kodi's Python API
  (dialog.select returns -1 for both "Back" and "torn down"; the keyboard's
  isConfirmed() is False for both), and the old flow treated a teardown as an
  answer, cleared the once-only marker and moved on. The device-name step now
  re-presents a non-answer instead of advancing, carries half-typed text forward
  into the re-presented keyboard, and only an explicit Keep or a confirmed entry
  ends it. Bounded to five attempts so an unattended box cannot spin.
- Fixed the cause, not just the symptom: the boot service prompted as soon as the
  home window was visible, which is the exact moment skin.estuary7's first-boot
  menu rebuild arms a 15-second timer. That rebuild ends in ReloadSkin(), which
  destroys the whole window stack. Reproduced twice on a local Kodi 21.3 bench at
  15.05s and 15.45s, killing an open prompt 15.27 seconds in. The service now
  waits past that deferred build, and honours skinshortcuts' own in-progress flag,
  before opening anything.
- Version bumped because the storage contract changed. The previous branch state
  carried modified nsud.py/wiz.py while still claiming to be 2026.07.17.7, which
  would have let a device-verification run certify code the box was not actually
  running, since verify_device.py compares version strings.

v1.0
Initial Release

v2020.12.19.1
- The plugin was also installed under Video add-ons. Now it only is in Program add-ons where it belongs
- Clean removed some files created by Common plugin cache or StorageServer or script.common.plugin.cache at kodi startup.
  This resulted in breaking cache functionality of kodi
- Upload kodi log file to pastebin didn't always work and the first bytes were binary characters
- Made all decode calls UTF-8
- Improved ADVANCED SETTINGS(BUFFER SIZE) functionality

v2021.11.03.0
- Fix Speedtest crash.

v2021.12.19.0
 - Made kodi 19 (Matrix) compatible.

v2022.12.28.0
- Fix backup. Crashed when foreign like Hebrew characters are used in xml files

2023.01.01.0
 - Fix a fix on 2022.12.28.0

2023.01.09.0
 - Added AutoClean Cache functionality every x days at a given hour.
 - Backup always replaced the home folder reference in xml files with special://home
   Not all addons can handle this replace so it is now possible via a setting to disable this.
 - Backup Cancel didn't work. This is now fixed.

2026.04.05.0
 - Updated for Kodi 22

2026.07.15.0
 - First-ever run now offers the same two-step tune-up a restore does: name this
   device, then size the video cache buffer for it. Exactly-once per install;
   boxes that already ran EZ Maintenance++ are not re-prompted by the update.

2026.07.15.1
 - Backup now confirms the final backup name (with its auto timestamp) before it
   starts, matching the restore confirm. Cancelling that prompt writes nothing.
   Applies to local, network, and Dropbox backups.

2026.07.15.2
 - Maintenance: new "Clear All" runs cache, packages, thumbnails, and (if any
   are found) the recently played channels in one action.
 - Maintenance: new "Clear Recently Played Channels" resets the PVR recently-
   played list. The write happens with the PVR manager paused so the running
   client cannot clobber it; it is a no-op with a notice when none are found.

2026.07.15.3
 - Clearing recently played channels now offers a Kodi restart. The home-screen
   widget reads the PVR "last played" list from the running manager's memory, so
   the cleared list only shows after Kodi reloads.

2026.07.15.4
 - Apple TV: the restart prompt now uses the honest "close and reopen Kodi"
   wording on tvOS. It was detected only as non-Android and mislabelled
   "Restart now?", but Kodi cannot self-restart on tvOS (Quit only closes).
   Fixes the wording for the new channel-clear restart offer and for every
   restore / Fresh Start prompt on Apple TV.

2026.07.16.0
 - Full backup now truly means full. The 2026.07.08.5 pvr.iptvsimple exclusion
   is reversed: IPTV settings (instance-settings, settings.xml, custom groups)
   are captured on BOTH Apple TV (from NSUserDefaults) and Fire TV, and a
   restore brings them back exactly as backed up. Stray instance files the
   archive does not carry are swept AFTER the extract (a canceled restore can
   never cost the box the config it already had), with every removal verified.
 - Restores briefly pause the IPTV client for the extract window and always
   re-enable it, so the live client can no longer overwrite the restored IPTV
   settings at shutdown.
 - Every backup embeds backup_manifest.json (what it holds, what it could not
   capture); restore verifies the archive against it. All backup / restore /
   wipe / Fresh Start reporting is now honest: partial results are reported as
   INCOMPLETE with the exact items, never "Complete", and an incomplete backup
   never rotates out the last good one.
 - Wipes on Apple TV clear BOTH storage layers (files and NSUserDefaults keys,
   verified against the store), and a one-time service migration purges stale
   hidden settings copies left by 2026.07.08.x-2026.07.13.x, materializing any
   key-only file to disk first so nothing is ever lost.
 - New Tools menu: "Purge stale tvOS keys" and "Verify backup archive".

2026.07.16.1
 - Verified live on real hardware (Fire TV + Apple TV): full backup captures
   IPTV on both OSes, restore brings it back exactly (strays swept, IPTV client
   paused and resumed), honest reporting confirmed. No functional change from
   2026.07.16.0 - this release records the hardware verification and the move to
   CI-driven publishing.

v2026.07.17.0
- Restore verifies its own work before reporting: a new "Verifying backup..." step triages
  everything the pre-restore wipe left behind against the backup (overwritten / harmless
  residue / dangerous stale keys) and auto-fixes the dangerous class with one silent fresh
  pass. The old mid-flight warning ("could not remove N items; leftovers may shadow...") is
  gone - it alarmed on states that were provably harmless and its modal could eat other
  prompts (this is exactly how a restored Apple TV came back in the stock skin).
- The user-facing vocabulary is now four owner-approved messages, with every count, path,
  and phase name demoted to the log: "Restore Complete" (the normal case), a Problem
  prompt with Try Again for genuine restore failures, a needs-attention line when a fix
  did not hold, and a silent post-restart self-check that only speaks on a real finding.
  "Restore INCOMPLETE" no longer appears for wipe imperfections - the restore phase and
  the wipe phase are reported as what they are. restore() now returns a structured summary
  (extracted/failed/unmapped/attention) so tooling keeps the full truth.
- Try Again re-runs the entire restore (fresh wipe included) from the already-staged
  backup, twice at most, then escalates instead of looping.
- The restored skin comes back reliably: lookandfeel.skin is no longer live-applied
  mid-restore (its keep-skin countdown was unanswerable behind our dialogs and reverted
  the skin). It is switched after all dialogs close and the confirmation is answered
  automatically, then verified.
- New restorecheck module (read-only verification: leftover triage, stale-key re-read,
  two-layer duplicate probe) shared by the restore and the boot self-check.

v2026.07.17.1
- Fix: the restored skin now reliably goes live on Apple TV after a restore.
  The v2026.07.17.0 cut answered Kodi's keep-this-skin dialog with SendClick(11)
  alone, which did not land on the Apple TV, so the box reverted to the stock
  skin (hardware-reproduced on atv2). _apply_boot_skin now falls back to remote
  navigation (proven to confirm on the device), VERIFIES the skin actually stuck
  after a settle, and retries several strategies before giving up loudly. New
  regression test reproduces the SendClick failure and proves the recovery.

v2026.07.17.2
- Fix: the post-restore verification no longer raises "needs attention" for a
  stale NSUserDefaults key the wipe could not clear when that key is at a path the
  backup does not restore - it shadows nothing restored, so it is cruft, logged not
  alarmed (hardware-repro atv2 2026-07-17: a clean restore falsely said "needs
  attention"). The genuine signal - a RESTORED file shadowed by a stale key - is a
  duplicate two-layer listing, which remains the attention case.

v2026.07.17.3
- Fix: the tvOS settings re-vector (a best-effort durability step after a restore) no
  longer raises "needs attention" when it misses some files. apply_guisettings has
  already applied the settings live (which persists on tvOS), so a re-vector miss is
  not a restore failure - it is logged. atv2 2026-07-17: a perfect restore kept saying
  "needs attention" because of this. Genuine functional failures (settings apply,
  IPTV pause/resume, stray-instance sweep, duplicate two-layer listings) still report.

v2026.07.17.4
- Fix: "needs attention" is now reserved for a genuinely broken restore. tvOS
  best-effort housekeeping that does not affect the restored box (a re-vector miss,
  an unclearable stale key not in the archive, an IPTV pause miss that did not
  materialize, a diagnostic probe that hiccuped) is LOGGED, not raised to the user.
  atv2 2026-07-17: a perfect restore kept saying "needs attention" over this noise.
  The signals that still report: settings-apply failure, IPTV re-enable failure,
  stray/duplicate IPTV instances, and a restored file shadowed by a stale key.
- The restore now publishes its verdict as a Home window property
  (ezm_restore_verdict / ezm_restore_findings) so it is readable over JSON-RPC for
  diagnostics, since this appliance's webserver /vfs refuses the log.

v2026.07.17.5
- Skin persistence rewrite: after a restore the restored skin is captured from the
  archive's guisettings BEFORE anything can clobber it, then written directly to disk
  and the tvOS NSUserDefaults layer as the LAST step - so the force-quit reopen loads
  it with no keep-this-skin dialog. Removes the flaky live-switch+SendClick confirm
  that reverted the Apple TV to stock ~1 in 4 restores. Includes a tvOS re-materialize
  guard so the write never blanks other settings.
- Silent-loss fix (merge/Dropbox path): a tvOS re-vector miss that could leave a stale
  key shadowing a restored file now reports needs-attention instead of a false
  "Complete". On the wipe path (keys already cleared) it stays harmless/log-only.
- A hard content failure surfacing on the auto-fix retry now shows the Problem wording,
  not the softer needs-attention.
- A wiped box is guaranteed to reach the restart prompt even on an unexpected error.
- Fresh Start: fixed an unpack bug that wiped the box then falsely reported the wipe
  did not run and skipped the restart. Now completes and restarts correctly.
- Set-up-this-box: the repo source rename to .T7B dedups by URL (no duplicate entries).
- Reviewed by three independent agents; the skin boot still owes one on-device run.

v2026.07.17.6
- Ended the boot-time war with skin.estuary7 1.0.66: the stale NSUserDefaults key
  purge now treats the skin's skinshortcuts *.DATA.xml keys (master profile AND
  per-profile) as skin-owned durability sidecars and never purges them, in either
  layer.
- Root-caused and fixed the purge's "failed" counting: post-delete verification
  re-read the NSUserDefaults backing plist, which cfprefsd flushes lazily on tvOS,
  so every successful key drop looked failed, the run-once marker never set, and
  the purge re-ran (and re-deleted the skin's keys) every boot. Verification now
  reads the LIVE layer via the xbmcvfs.listdir dup-count (a name listed twice has
  a surviving key; once means the key is gone).
- Only-copy safety tightened: a directory squatting on a key's path no longer
  counts as a disk twin (the key would have been the only real copy).
- Log viewer / pastebin uploader: crash-damaged logs (invalid UTF-8 written during
  a hard crash) no longer make both die silently; the bytes are replaced with
  markers and the log still displays/uploads. Cancel no longer picks the last log
  by accident, an empty log list says so instead of doing nothing, upload failures
  close the stuck busy dialog and are logged.
- Reviewed by two fix agents and two adversarial QA agents; tvOS hardware
  verification on atv2 recorded in verification/2026.07.17.6.json (android class
  waived: the changed storage path is tvOS-gated and never executes on Fire OS).

v2026.07.17.7
- Fixed the restore-time stray IPTV-instance sweep's false-failure reporting:
  it verified key drops by re-reading the NSUserDefaults backing plist, which
  cfprefsd flushes lazily on tvOS, so a successful drop looked failed and a clean
  restore could report PARTIAL / "needs attention". Verification now reads the
  LIVE key layer via the xbmcvfs.listdir dup-count, the same doctrine the
  v2026.07.17.6 boot purge fix uses. No change to what the sweep deletes or keeps;
  the duplicate-instance brick guard is unchanged, a genuinely surviving key is
  still detected and reported.
- Reviewed by one adversarial QA agent; its test-fidelity finding (the two-layer
  fake modelled a missing directory as still observable) was fixed so a stuck key
  in that shape cannot pass green. tvOS hardware verification on atv2 recorded in
  verification/2026.07.17.7.json (android waived: the sweep's key layer is
  tvOS-only).
